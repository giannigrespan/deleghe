# Minimal requirements for Python 3.14 (Windows)
# Skips OCR dependencies that have build issues

# PDF Processing (without OCR)
PyPDF2>=3.0.1
pdfplumber>=0.11.0
# pdf2image requires Pillow, skipped for now

# Data processing
pandas>=2.2.0
openpyxl>=3.1.5

# CLI and utilities
click>=8.1.7
python-dotenv>=1.0.0
colorama>=0.4.6

# Reporting
jinja2>=3.1.4

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
